Get GoogleScholar info from a database with lookup metadata:

INSTALL:
```bash
sudo pip3 install git+https://github.com/colav/redalyc.git#egg=redalyc
```

Lookup search example: http://bit.ly/gslookup

